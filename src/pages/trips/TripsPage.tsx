import { ModulePlaceholder } from '../module/Placeholder'

export function TripsPage() {
  return (
    <ModulePlaceholder
      title="Trips"
      description="Trip lifecycle: scheduling, routing, driver/vehicle assignment, status tracking (SCHEDULED → COMPLETED), passenger manifests, mobility needs, and trip notes. Model designed without PII so passenger data can attach via secured subcollections in a later phase."
    />
  )
}
